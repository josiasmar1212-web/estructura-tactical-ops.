import streamlit as st
import pandas as pd

# CONFIGURACIÓN DE PÁGINA PROFESIONAL
st.set_page_config(page_title="ESTRUTURA TACTICAL", page_icon="🏗️", layout="wide")

# CSS ESTILO INDUSTRIAL / TACTICAL
st.markdown("""
    <style>
    .stApp { background-color: #0b0c10; color: #f1c40f; }
    .header-box {
        background-color: #f1c40f; color: black; padding: 30px;
        text-align: center; border-radius: 0 0 20px 20px;
        font-family: 'Impact'; letter-spacing: 3px;
    }
    .stButton>button {
        background-color: #f1c40f; color: black; border-radius: 5px;
        font-weight: bold; width: 100%; border: none;
    }
    </style>
""", unsafe_allow_html=True)

st.markdown('<div class="header-box"><h1>ESTRUTURA: TACTICAL OPS v1.0</h1></div>', unsafe_allow_html=True)
st.write("")

# LÓGICA DE SIMULACIÓN
col1, col2 = st.columns(2)

with col1:
    st.subheader("📋 Baremos de Examen")
    with st.form("form_opos"):
        cuerpo = st.selectbox("Oposición", ["Policía Nacional", "Guardia Civil", "Bomberos"])
        circuito = st.number_input("Circuito (seg)", 7.0, 15.0, 10.0)
        domis = st.slider("Dominadas", 0, 20, 10)
        btn = st.form_submit_button("CALCULAR APTITUD")

with col2:
    if btn:
        # Cálculo de ejemplo para mostrar resultados
        nota = round((11 - (circuito - 7)) + (domis * 0.5), 1)
        st.metric(label="PUNTUACIÓN TOTAL", value=f"{nota} pts")
        
        if nota >= 15:
            st.success("ESTADO: APTO PARA EL CUERPO")
        else:
            st.error("ESTADO: NO APTO - MEJORA TUS MARCAS")
    else:
        st.info("Introduce tus datos para iniciar la evaluación táctica.")

st.markdown("---")
st.caption("© 2026 ESTRUTURA | Tactical Solutions por Josías Martínez")
