import streamlit as st

# 1. Configuración básica de la página
st.set_page_config(page_title="ESTRUTURA TACTICAL", layout="centered")

# 2. Estilo visual rápido
st.markdown("""
    <style>
    .reportview-container { background: #0e1117; }
    .main-title { color: #f1c40f; font-family: 'Courier New'; text-align: center; border: 2px solid #f1c40f; padding: 10px; }
    </style>
""", unsafe_allow_html=True)

# 3. Contenido
st.markdown('<h1 class="main-title">ESTRUTURA: TACTICAL OPS</h1>', unsafe_allow_html=True)
st.write("---")

st.subheader("Simulador de Aptitud Física")
nombre = st.text_input("Nombre del Opositor", "Josías Martínez")
puntos = st.slider("Puntuación Estimada", 0, 30, 15)

if st.button("VERIFICAR ESTADO"):
    if puntos >= 15:
        st.success(f"OPERADOR {nombre}: APTO PARA SERVICIO")
    else:
        st.error(f"OPERADOR {nombre}: NO APTO - SE REQUIERE ENTRENAMIENTO")

st.info("⚠️ Asegúrate de que el archivo requirements.txt esté en tu GitHub con la palabra 'streamlit'.")
